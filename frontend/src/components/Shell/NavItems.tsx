/** Navigation definitions with i18n keys — use `localizeNav` from `../../i18n/nav` with `useTranslation`. */
export {
  CONFIG_NAV_DEF,
  MAIN_NAV_DEF,
  MORE_NAV_DEF,
  WORKSPACE_NAV_DEF,
  localizeNav,
  type NavItem,
  type NavItemDef,
} from '../../i18n/nav'
