apiVersion: v1
kind: Service
metadata:
  name: {{ include "goloom.fullname" . }}
  labels:
    {{- include "goloom.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goloom.selectorLabels" . | nindent 4 }}
